<h1>New admin_description</h1>

<%= form_for @admin_description, html: { class: "form-horizontal", role: "form" } do |f| %>
  <% if @admin_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_description.errors.count, "error") %> prohibited this admin_description from being saved:</h2>

      <ul>
      <% @admin_description.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :text, class: "col-sm-2 control-label"  %>
    <div class="col-sm-10">
      <%= f.text_area :text, class: 'form-control', rows: 25 %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit 'Save', class: "btn btn-primary" %>
    </div>
  </div>
<% end %>


<%= link_to 'Back', admin_descriptions_path %>
